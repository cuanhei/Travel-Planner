SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key

# Google API key — shared by every Google API this app calls (enable
# each one for this key/project in Google Cloud Console):
# - Routes API (computeRoutes): Transport module's transit/driving directions.
# - Places API (New): Explore tab's Nearby Places section.
GOOGLE_ROUTES_API_KEY=your-google-api-key
